#include<stdio.h>
int bubbleSort(arr, n) {
    int max = arr[0];
    for (int i=0;i<n;i++) {
        if (arr[i]>max) {
            max = array[i];
        }
    }
    return 0;
}
int printArray(arr, n) {
    printf("%d ",max);
    return 0;
}